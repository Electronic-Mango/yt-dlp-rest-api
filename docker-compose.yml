services:
  ytdlp-rest-api:
    image: ytdlp-rest-api:latest
    build: .
    container_name: ytdlp-rest-api
    restart: unless-stopped
    env_file: .env

networks:
  default:
    name: ytdlp-rest-api-network
    driver: bridge
