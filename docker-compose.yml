services:
  yt-dlp-rest-api:
    image: yt-dlp-rest-api:latest
    build: .
    container_name: yt-dlp-rest-api
    restart: unless-stopped
    env_file: .env

networks:
  default:
    name: yt-dlp-rest-api-network
    driver: bridge
